/*
SQL General Rules
- Use underscore_names instead of CamelCase
Table names must be plural
Spell out ID fiels i.e. (user_id instead of id)
Don't use ambigous column names
Name foureing key columns the same as the columns they refer to

*/